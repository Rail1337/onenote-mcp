[project]
name = "onenote"
version = "1.0.0"
description = "MCP server for reading and writing local Microsoft OneNote notebooks (local backups + live COM API)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.26.0",
    "pyonenote>=0.0.2",
]
