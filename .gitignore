# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Token cache (contains auth secrets)
.onenote_token_cache.json

# Packed extension bundles (build artifacts)
*.mcpb

# Personal action history (not for the public repo)
history.md
history.archive.md

# Session work-log (context for us, not user-facing docs)
session-log.txt
